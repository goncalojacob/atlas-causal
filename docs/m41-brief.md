# Build brief — M41: a second Portuguese round, filtered for consequence

Owner's request, 5 September 2026. Runs after M40b on branch `world`
(the same branch, so one merge). The previous import was two thirds
ballots (M21/M22 retracted 122 of 170). This round asks Wikidata for
Portuguese events 1890–2025 **by classes that carry consequence** — law,
treaty, strike, protest, crisis, institution founded, company founded or
nationalised, infrastructure opened, disaster, assassination, trial,
death in office, referendum, constitution, currency change — and
excludes election classes entirely (the seeds' `classes` table says
which are refused). M41a: the queries, the Action, `docs/m41-candidates.md`,
ticked by the same rule as M40a (top 150 by sitelinks not already here,
at least eight per decade), imported, leads cached. M41b: drafted as
M40b, edges to what is here, retractions in `docs/m41-retractions.md`.
Done when: the two literal lines `M41a done` and `M41b done`.
