# Build brief — M42: the world at scale, from Wikipedia (outline)

Owner's request, 5 September 2026: "do not use synthetic events, go on
Wikipedia and complete with real events all over the world" — **after
everything else** (the health cycle, M30–M39, M40, M41). This is an
outline; the brief is written when its turn comes, against the spine.

- Thousands of events, every region, 1890–2025 first and earlier
  periods after, through the Wikidata import by class and period with
  sitelinks as the rank, imported in batches the Action can finish, each
  batch merged with an index rebuild.
- Records carry `origin: wikidata`, `review.flags: ["imported-facts"]`,
  a summary quoting the description, identity fields, actors only where
  Wikidata gives participants — and **no drafted edges at this scale**:
  the point is to stress the spine, the shards, the search, the
  clustering, the layout, the validator and the dashboard with real
  records; edges come from people and from the Why mode's later work.
- The measures from `docs/health-review-2026-09-05-B.md`'s table are
  re-taken on the real data at each order of magnitude and recorded.
